# Google Gemini API Key
# Get your key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_api_key_here

# =============================================================================
# API Server Configuration
# =============================================================================

# Port for the API server (default: 23456)
API_PORT=23456

# Host for the API server (default: 127.0.0.1 for localhost only)
# Use 0.0.0.0 to allow external connections (not recommended for production)
API_HOST=127.0.0.1

# Logging level for API server
# Options: critical, error, warning, info, debug
API_LOG_LEVEL=info

# Enable CORS for specific origins (comma-separated)
# Example: http://localhost:3000,http://localhost:8080
# Leave empty to allow all origins (not recommended for production)
API_CORS_ORIGINS=
